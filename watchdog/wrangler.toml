name = "devbox-watchdog"
main = "src/index.ts"
compatibility_date = "2025-09-06"

# 每 15 分鐘檢查一次指標。
[triggers]
crons = ["*/15 * * * *"]

# 必要 secrets(勿寫在此檔,用指令設定):
#   wrangler secret put TELEGRAM_TOKEN
#   wrangler secret put TELEGRAM_CHAT_ID
